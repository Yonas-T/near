var nearNominationExp = 24;

/// Convert account balance value from internal indivisible units to NEAR. 1 NEAR is defined by {@link NEAR_NOMINATION}.
/// Effectively this divides given amount by {@link NEAR_NOMINATION}.
 
/// balance decimal string representing balance in smallest non-divisible NEAR units (as specified by {@link NEAR_NOMINATION})
/// fracDigits number of fractional digits to preserve in formatted string. Balance is rounded to match given number of digits.
/// @returns Value in Ⓝ
formatNearAmount(balance, nearNominationExp) {
  
}
